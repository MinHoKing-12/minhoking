DROP TABLE FREE_BBS
CASCADE CONSTRAINTS;

DROP SEQUENCE SEQ_FREE_BBS;

CREATE TABLE FREE_BBS(
   SEQ NUMBER(8) NOT NULL PRIMARY KEY,
   ID VARCHAR2(20) NOT NULL,
   NICKNAME VARCHAR2(20) NOT NULL,
   TITLE VARCHAR2(200) NOT NULL,
   CONTENT VARCHAR2(4000) NOT NULL,
   WDATE DATE NOT NULL,
   DEL NUMBER(1) NOT NULL,
   READCOUNT NUMBER(4) NOT NULL,
   BLIKE NUMBER(8) NOT NULL
)

CREATE SEQUENCE SEQ_FREE_BBS  
START WITH 1
INCREMENT BY 1;

ALTER TABLE FREE_BBS
ADD CONSTRAINT FK_FREE_BBS FOREIGN KEY(ID)
REFERENCES MEMBER(ID);

SELECT * FROM MEMBER
SELECT * FROM FREE_BBS;
SELECT * FROM TB_FREE_COMMENT;

DROP TABLE TB_FREE_COMMENT;

ALTER TABLE FREE_BBS DROP PRIMARY KEY DROP INDEX;
ALTER TABLE MEMBER DROP PRIMARY KEY DROP INDEX;

ALTER TABLE FREE_BBS DROP CONSTRAINT FK_FREE_BBS CASCADE;
ALTER TABLE FREE_BBS DROP CONSTRAINT FK_TB_FREE_COMMENT CASCADE;
