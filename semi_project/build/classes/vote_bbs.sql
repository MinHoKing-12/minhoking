DROP TABLE VOTE_BBS
CASCADE CONSTRAINTS;

DROP SEQUENCE SEQ_VOTE;

CREATE TABLE VOTE_BBS(
SEQ NUMBER(8) PRIMARY KEY,
ID VARCHAR2(20) NOT NULL,
PLAYNUM NUMBER(8) NOT NULL
);



CREATE SEQUENCE SEQ_VOTE
START WITH 1
INCREMENT BY 1;

ALTER TABLE VOTE_BBS
ADD CONSTRAINT FK_VOTE_PLAYNUM FOREIGN KEY(PLAYNUM)
REFERENCES PLAYER_INFO(SEQ);


ALTER TABLE VOTE_BBS
ADD CONSTRAINT FK_VOTE_ID FOREIGN KEY(ID)
REFERENCES MEMBER(ID);








SELECT * FROM VOTE_BBS;

SELECT * FROM MEMBER;