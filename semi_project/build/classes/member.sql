DROP TABLE MEMBER
CASCADE CONSTRAINTS;

CREATE TABLE MEMBER(
	ID VARCHAR2(20) PRIMARY KEY,
	PWD VARCHAR2(16) NOT NULL,
	NICKNAME VARCHAR2(30) UNIQUE,
	NAME VARCHAR2(50) NOT NULL,
	POSTNUM NUMBER(5) NOT NULL,
	ADDRESS VARCHAR2(100) NOT NULL,
	ADDRESS_DETAIL VARCHAR2(100) NOT NULL,
	EMAIL VARCHAR2(50) UNIQUE,
	POINT NUMBER(10) NOT NULL,
	JDATE DATE NOT NULL,
	AUTH NUMBER(1) NOT NULL
);

DROP TABLE MEMBER_GRADE
CASCADE CONSTRAINTS;

CREATE TABLE MEMBER_GRADE(
	NAME VARCHAR2(20) PRIMARY KEY,
	MIN_POINT NUMBER(5) NOT NULL,
	MAX_POINT NUMBER(5) NOT NULL,
	IMG_NAME VARCHAR2(20) NOT NULL
);

UPDATE MEMBER
SET POINT = 3000
WHERE ID='kiwoomjjang';

INSERT INTO MEMBER_GRADE
VALUES('아마추어 선수', 0, 100, 'amature.png');
INSERT INTO MEMBER_GRADE
VALUES('프로 선수', 100, 500, 'pro.png');
INSERT INTO MEMBER_GRADE
VALUES('코치', 500, 2000, 'coach.png');
INSERT INTO MEMBER_GRADE
VALUES('감독', 2000, 5000, 'manager.png');
INSERT INTO MEMBER_GRADE
VALUES('구단주', 5000, 99999, 'boss.png');

SELECT G.NAME
FROM MEMBER M, MEMBER_GRADE G
WHERE M.POINT >= G.MIN_POINT
	AND M.POINT < G.MAX_POINT;

SELECT * FROM MEMBER_GRADE;

SELECT * FROM MEMBER;

INSERT INTO MEMBER
VALUES('manager', '1234', '관리자', '관리자', '06258', '서울특별시 강남구', '백암빌딩 5층', 'manager@naver.com', 10000, SYSDATE, 1);

SELECT C.SEQ
FROM SELL_COMMENT C, (SELECT SEQ FROM SELL_BBS WHERE ID='kiwoomjjang') B
WHERE PARENT = B.SEQ;
