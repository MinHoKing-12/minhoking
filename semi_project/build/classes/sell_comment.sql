DROP TABLE SELL_COMMENT
CASCADE CONSTRAINTS;

DROP SEQUENCE SELL_COMMENT_SEQ;

CREATE TABLE SELL_COMMENT(
	SEQ NUMBER(8) PRIMARY KEY,
	ID VARCHAR2(20) NOT NULL,
	CONTENT VARCHAR2(200) NOT NULL,
	REF NUMBER(20) NOT NULL,
	STEP NUMBER(20) NOT NULL,
	DEPTS NUMBER(20) NOT NULL,
	PARENT NUMBER(8) NOT NULL,
	DEL NUMBER(1) NOT NULL,
	LIKECOUNT NUMBER(8) NOT NULL,
	REPORT NUMBER(8) NOT NULL,
	WDATE DATE
);

CREATE SEQUENCE SELL_COMMENT_SEQ
START WITH 1
INCREMENT BY 1;

ALTER TABLE SELL_COMMENT
ADD CONSTRAINT FK_SELL_COMMENT2 FOREIGN KEY(ID)
REFERENCES MEMBER(ID);

SELECT * FROM SELL_COMMENT;

SELECT COUNT(*) FROM SELL_COMMENT WHERE ID='yeonha94';

