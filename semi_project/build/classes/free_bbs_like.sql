DROP SEQUENCE SEQ_FREE_BBS_LIKE;

DROP TABLE FREE_BBS_LIKE 
CASCADE CONSTRAINTS;

CREATE TABLE FREE_BBS_LIKE(
   SEQ NUMBER(8) PRIMARY KEY,
   PAGE NUMBER(8) NOT NULL,
   ID VARCHAR2(20) NOT NULL      
)   
 
CREATE SEQUENCE SEQ_FREE_BBS_LIKE  
START WITH 1
INCREMENT BY 1;

ALTER TABLE FREE_BBS_LIKE
ADD CONSTRAINT FK_FREE_BBS_LIKE FOREIGN KEY(PAGE)
REFERENCES FREE_BBS(SEQ);

ALTER TABLE FREE_BBS_LIKE
ADD CONSTRAINT FK_FREE_BBS_LIKE2 FOREIGN KEY(ID)
REFERENCES MEMBER(ID);

SELECT * FROM FREE_BBS_LIKE

SELECT B.SEQ, B.ID, B.NICKNAME, TITLE, CONTENT, WDATE, DEL, READCOUNT, BLIKE
FROM FREE_BBS B, (SELECT PAGE FROM FREE_BBS_LIKE WHERE ID='yeonha94') C
WHERE B.SEQ = C.PAGE AND B.DEL = 0;


